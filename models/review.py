#!/usr/bin/python3
""" Review Module """

from models.base_model import BaseModel

class Review(BaseModel)
	""" Inherits from BaseModel
	Attributes:
		place_id (str): Stores the <place_id>
		user_id (str): Stores the <user_id>
		text (str): Review text
	"""

	place_id = user_id = text = ""
